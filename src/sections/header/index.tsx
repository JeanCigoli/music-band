import React from 'react';

import { Section, Container, NavBar } from './styled';

const Headers = () => {
  return (
    <Section id="header">
      <NavBar>Aqui</NavBar>
      <Container>Aqui fica o centro</Container>
    </Section>
  );
};

export default Headers;
